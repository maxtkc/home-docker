http:
  services:
    tgtg:
      loadBalancer:
        servers:
          - url: "http://nextcloud_tgtg_1:3000"

  routers:
    tgtg:
      rule: "Host(`tgtg.kcfam.us`)"
      entryPoints:
        - "websecure"
      tls:
        certResolver: letsencrypt
      service: "tgtg"
